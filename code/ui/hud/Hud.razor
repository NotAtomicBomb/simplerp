@using Sandbox;
@using Sandbox.UI;

@namespace MyGame
@inherits RootPanel
@attribute [StyleSheet]


<root>
    <ChatBox/>
    <VoiceList/>
    <VoiceSpeaker/>
    <PlayerInfo/>
    @if((Game.LocalPawn as Player).ActiveWeapon != null)
    {
        <WeaponInfo/>
    }
    <JobMenu/>
    <PlayerList/>
    <Crosshair/>
</root>

@code
{

}
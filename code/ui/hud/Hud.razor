@using Sandbox;
@using Sandbox.UI;
@using System.Linq;

@namespace MyGame
@inherits RootPanel
@attribute [StyleSheet]


<root>
    <ChatBox/>
    <VoiceList/>
    <VoiceSpeaker/>
    <PlayerInfo/>
    @if((Game.LocalPawn as Player).ActiveChild != null)
    {
        <WeaponInfo/>
    }
    <JobMenu/>
    <PlayerList/>
    <Crosshair/>
    <InventoryBar/>
</root>

@code
{
    public Hud()
    {
        Log.Info("Hud created.");
    }
}
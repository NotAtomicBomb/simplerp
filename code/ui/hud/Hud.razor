@using Sandbox;
@using Sandbox.UI;

@namespace MyGame
@inherits RootPanel
@attribute [StyleSheet]


<root>
    <ChatBox/>
    <VoiceList/>
    <PlayerInfo/>
    @if((Game.LocalPawn as Player).ActiveWeapon != null)
    {
        <WeaponInfo/>
    }
</root>

@code
{

}